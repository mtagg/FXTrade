##potential executable functions 
"""                               
     oporder.create_order() 
     oporder.get_pending_orders()       ##requires order id
     oporder.replace_order()            ##requires order id
     oporder.get_order_details()        ##requires order id
     oporder.cancel_pending_order()     ##requires order id
     oporder.replace_order()            ##requires order id

     #oporder.replace_order(account_id=CONFIG.account_id, order_id=None, order=None)
     #optrades.update_trade(account_id=CONFIG.account_id, order_id=None)

     optrans.get_transaction_details    ##requires transaction id
     optrans.get_transactions

     oppos.close_position
     oppos.get_open_positions
     oppos.get_positions



"""